# Copy to .env and adjust before `docker compose up`.

# Postgres
POSTGRES_USER=atrium
POSTGRES_PASSWORD=atrium
POSTGRES_DB=atrium

# App
PORT=8080

# Seeded admin account (change the password before deploying)
SEED_USER_EMAIL=eve@frieswings.com
SEED_USER_PASSWORD=atrium
SEED_USER_NAME=Eve Holt
